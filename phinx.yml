paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: mysql
    mysql:
        adapter: mysql
        host: 127.0.0.1
        name: webhome-user
        user: root
        pass: my-secret-pw
        port: 3306
        charset: utf8
