{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ 'users.title.add'|trans }}{% endblock %}

{% block breadcrumb %}
    {{ include('NdewezWebHomeCommonBundle::breadcrumb.thml.twig', {
            'links': [
                {'title': 'home.title.home', 'route': 'app_home'},
                {'title': 'users.title.list', 'route': 'app_users_list'},
                {'title': 'users.title.add', 'active': true},
            ]
        })
    }}
{% endblock %}

{% block body %}
    <h2>{{ 'users.title.add'|trans }}</h2>

    {{ form(form) }}

{% endblock %}
